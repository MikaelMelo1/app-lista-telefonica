/*import React, { Component } from "react";
import { StyleSheet, View, Text, Image } from "react-native";

export default class Lista extends Component {
  render() {
    return (
      <View style={styles.containerLista}>
        <Text style={styles.titleLista}>{this.props.data.nome}</Text>
        <Text style={styles.titleLista}>{this.props.data.numero}</Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({
  containerLista: {
    backgroundColor: "orange",
    height: 80,
    margin: 10,
    color: "white",
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 10,
  },
  titleLista: {
    color: "white",
    fontSize: 20,
    fontWeight: "bold",
  },
  lugar: {
    height: 20,
    width: 20,
    backgroundColor: "black",
  },
}); */
